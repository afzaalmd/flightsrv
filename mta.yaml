_schema-version: "3.1"
ID: flightsrv
description: A simple CAP project.
version: 1.0.0
modules:
- name: flightsrv-srv
  type: nodejs
  path: gen/srv
  requires:
  - name: flightsrv-auth
  - name: flightsrv-db
  provides:
  - name: srv-api
    properties:
      srv-url: ${default-url}
  parameters:
    buildpack: nodejs_buildpack
    disk-usuage: 256M
    memory: 256M
    readiness-health-check-http-endpoint: /health
    readiness-health-check-type: http
  build-parameters:
    builder: npm
- name: flightsrv-db-deployer
  type: hdb
  path: gen/db
  requires:
  - name: flightsrv-db
  parameters:
    buildpack: nodejs_buildpack
    disk-usuage: 256M
    memory: 256M
- name: flightsrv-app-content
  type: com.sap.application.content
  path: .
  requires:
  - name: flightsrv-repo-host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - flightapp.zip
      name: flightapp
      target-path: resources/
- name: flightapp
  type: html5
  path: app/flightapp
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
resources:
- name: flightsrv-auth
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      tenant-mode: dedicated
      xsappname: flightsrv-${org}-${space}
    path: ./xs-security.json
    service: xsuaa
    service-plan: application
- name: flightsrv-db
  type: com.sap.xs.hdi-container
  parameters:
    service: hana
    service-plan: hdi-shared
- name: flightsrv-repo-host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-name: flightsrv-html5-srv
    service-plan: app-host
- name: flightsrv-destination-service
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: false
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          existing_destinations_policy: update
      version: 1.0.0
    service: destination
    service-name: flightsrv-destination-service
    service-plan: lite
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
build-parameters:
  before-all:
  - builder: custom
    commands:
    - npm ci
    - npx cds build --production
